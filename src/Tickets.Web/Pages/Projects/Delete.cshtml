@page
@model Tickets.Web.Pages.Projects.DeleteModel
@{
}
<div class="mx-auto" style="width:1100px;">
    <h1>Delete</h1>

    <h3>Are you sure you want to delete this?</h3>
    <div>
        <h4>Project #@Model.Project.Id</h4>
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Project.Title)
            </dt>
            <dd class="col-sm-2">
                @Html.DisplayFor(model => model.Project.Title)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Project.ProjectType)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Project.ProjectType)
            </dd>

            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Project.ClientName)
            </dt>
            <dd class="col-sm-2">
                @Html.DisplayFor(model => model.Project.ClientName)
            </dd>

        </dl>

        <div asp-action="Delete">
            <a class="btn btn-danger" asp-route-id="@Model.Project.Id">Delete</a>
            <a asp-page="Index" class="btn btn-outline-dark">Back to List</a>
        </div>
    </div>
</div>